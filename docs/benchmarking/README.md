# Benchmarking

Documentation for the `benchmarks/` directory (Criterion-style
benchmarks), referenced from `docs/ai/Coach.md`'s note on shot-evaluation
performance budgets and RFC 0005's open question on evaluation speed.

## Planned contents

- `BenchmarkingGuide.md` — how to write and run a benchmark
- `PerformanceBudgets.md` — target performance for hot paths (physics
  step, shot evaluation) once measured

Status: empty — populate once `benchmarks/` has its first real benchmark
(tracked alongside RFC 0001 implementation).
