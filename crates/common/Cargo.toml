[package]
name = "cueforge-common"
version = "0.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Shared types: units (Meters, Seconds, ...), seeded RNG, math helpers."

[dependencies]
# Dependencies added here must respect the one-way crate dependency graph
# documented in docs/architecture/DataFlow.md. Adding a dependency to
# this crate if it is physics/collision/spin requires an RFC (see
# AGENTS.md).
