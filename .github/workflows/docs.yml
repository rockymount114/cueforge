name: Docs

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check-docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check for broken relative links in docs/
        run: |
          echo "TODO: wire up a markdown link checker (e.g. lychee) here"
      - name: "Check for TODO: verify count (informational)"
        run: |
          grep -r "TODO: verify" docs/ || true
